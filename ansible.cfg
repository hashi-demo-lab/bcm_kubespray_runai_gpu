# Minimal Ansible configuration for user creation playbook
# This overrides the system /etc/ansible/ansible.cfg to avoid vault issues

[defaults]
# Basic settings
host_key_checking = False
retry_files_enabled = False
gathering = smart

[ssh_connection]
ssh_args = -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null
